# Unit Test Instructions — U3 第 1 部（`u3-context`）

```bash
cd /home/user/learn-AI-DLCv2 && npm run build:first && node scripts/check-first.mjs --no-external 2>&1 | grep -E "01-context|blocking"
```

合否: 1.1 に関する BLOCKING 0 件、本文 2,000〜6,000 文字、出典行 1 行以上、claude 版へのリンクが `_site/claude/` に解決すること。
